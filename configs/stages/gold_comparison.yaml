gold_comparison:
  layer_name: gold
  recipe_id: gold_modeling__v001_model_comparison

  process_run_id_prefix: gold_comparison

  artifact_layout:
    stage: gold
    family: comparison
    subdirs:
      - results
      - plots
      - summaries
      - statistics
      - metadata
      - lineage