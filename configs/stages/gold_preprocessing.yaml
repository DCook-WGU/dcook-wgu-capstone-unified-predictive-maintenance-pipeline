gold_preprocessing:
  layer_name: gold
  recipe_id: gold_preprocessing__v001_cascade
  
  versions:
    gold: gold__preprocessing__v001
    truth: truth_v001

  process_run_id_prefix: gold_process

  artifact_layout:
    stage: gold
    family: preprocessing
    subdirs:
      - features
      - profiles
      - models
      - summaries
      - metadata
      - config
      - lineage
      
  train_fraction: 0.70
  random_seed: 42
  scaler_kind: robust
  scaler_artifact_name_template: "{dataset.name}__gold__{gold_preprocessing.scaler_kind}_scaler.joblib"
  stage2_target_feature_count: 12
  stage3_primary_count: 5
  stage3_secondary_count: 8